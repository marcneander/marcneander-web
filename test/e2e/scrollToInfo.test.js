/**
 * Scroll users down to the info section should work
 */

module.exports = {
    'Scrolls users down to the info section.': browser => {
        // Todo: Write the actual test
        browser.url('http://web').end();
    }
};
